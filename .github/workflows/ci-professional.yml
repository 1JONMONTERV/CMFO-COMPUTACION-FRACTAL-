name: CMFO CI with Professional Tests

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    name: Test on ${{ matrix.os }} (Python ${{ matrix.python-version }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        python-version: ['3.9', '3.10', '3.11', '3.12']

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}
        cache: 'pip'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pytest pytest-cov numpy

    - name: Install CMFO package
      working-directory: bindings/python
      run: |
        pip install -e .

    - name: Run tests with coverage
      run: |
        pytest tests/ -v --cov=cmfo --cov-report=xml --cov-report=term

    - name: Upload coverage to Codecov
      uses: codecov/codecov-action@v3
      with:
        file: ./coverage.xml
        flags: unittests
        name: codecov-umbrella
        fail_ci_if_error: false

  lint:
    name: Lint and Format Check
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install linting tools
      run: |
        python -m pip install --upgrade pip
        pip install black flake8 mypy

    - name: Check formatting with black
      working-directory: bindings/python
      run: |
        black --check cmfo/ || echo "⚠️ Formatting issues found (non-blocking)"
      continue-on-error: true

    - name: Lint with flake8
      working-directory: bindings/python
      run: |
        flake8 cmfo/ --count --select=E9,F63,F7,F82 --show-source --statistics || echo "⚠️ Critical lint issues"
      continue-on-error: true
